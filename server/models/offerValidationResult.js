function offerValidationResult(id, bidAmount, isAccepted, status) {
    this.id = id; 
    this.bidAmount = bidAmount; 
    this.isAccepted = isAccepted; 
    this.status = status; 
}

module.exports = offerValidationResult; 
